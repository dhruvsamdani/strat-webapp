import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#4357ad",
    },
    background: {
      default: "#ebf3ff",
    },
  },
  shape: {
    borderRadius: 20,
  },
});

export default theme;
